[2026-09-07] | I treated missing spreadsheet fields as required migration data even though the sheet is only a designation and reporting-relationship reference. | Use the spreadsheet only to derive the requested designation catalog and reporting-officer flags; let administrators create and complete employee records manually unless the user explicitly asks for an import.
[2026-09-07] | I delivered a functional interface that was visually too basic for the requested professional HR system. | Treat polished information architecture, responsive navigation, visual hierarchy, and component-level design as first-class deliverables, not post-build decoration.
[2026-09-07] | I did not seed an employee profile for the test administrator, leaving the profile page broken, and I under-scoped document ownership and product navigation. | Seed a complete administrator profile and validate every primary navigation path; model admin document uploads against selectable employee owners and design the application around complete workflows, not isolated forms.
[2026-09-07] | I configured delivery email but left transactional messages as unbranded plain text. | Treat every customer-facing output, including email, as part of the product surface and implement a shared branded template system before calling an email feature complete.
[2026-09-07] | I added overlapping mobile navigation and a drawer override that displayed both logo variants without a dismissal interaction. | Validate every responsive state independently: mobile must have one navigation pattern, one correctly sized brand mark, and predictable close behaviour for transient panels.
[2026-09-07] | I corrected the duplicate theme logos but retained redundant product branding and did not expose a visible drawer close control. | Establish one explicit brand hierarchy per breakpoint: company logo in navigation, product identity once in the app bar, and an obvious close affordance inside every mobile drawer.
[2026-09-07] | I placed a mobile company wordmark beside the centred product identity, causing visual overlap in the narrow app bar. | At narrow widths, reserve the top bar for one centred product identity; keep company branding inside the opened drawer instead of duplicating it in the header.
[2026-09-07] | I normalized logo element dimensions without accounting for the dark source image’s large built-in canvas, so its visible wordmark remained smaller. | Size visual marks, not just image elements: use a shared clipping frame and compensate source-image whitespace where assets have different crops.
[2026-09-07] | I deployed CSRF protection without including CSRF tokens in the application’s POST forms, causing production login to fail with a 400 error. | Whenever request protection is enabled, exercise every form in the production configuration and render a shared CSRF field in each POST form.
[2026-09-07] | I returned a raw plain-text response for failed login attempts, bypassing the polished authentication interface. | Every expected authentication failure must render an accessible, branded error state within the same login flow.
[2026-09-07] | I provided a run module that created the application but did not start a development server when invoked with Python. | Every documented local entry point must be executable end-to-end, including database setup and a clear development-server command.
[2026-09-08] | I passed Flask's typed request accessor directly through a Jinja template, where the conversion function was undefined and employee searches crashed. | Parse and validate request inputs in routes, then pass plain display values into templates; do not invoke framework request parsing APIs from templates.
[2026-09-09] | I added a new runtime dependency only to the project virtual environment, while the documented `python run.py` path was commonly invoked through the user's base Python. | Verify the exact documented interpreter and launch command after adding every runtime dependency; either provision that interpreter or give an explicit environment activation command.
[2026-09-09] | I delivered core HR models without exposing the essential administrator workflows for leave balances, approvals, salary entry, public holidays, and employee-owned request edits. | Treat every core data model as incomplete until its authorized create, edit, approval, and audit-facing screens are reachable and tested from the intended user role.
[2026-09-09] | I used fixed leave/request approval logic and fixed HR request categories where the product needs administrator-managed workflow and service configuration. | Build approval routing and request types as versioned admin configuration, with runtime history retained for in-flight requests.
[2026-09-09] | I improved individual screens but left the product shell, navigation hierarchy, iconography, and page composition feeling like an accumulated internal tool instead of one premium HR platform. | When a user asks for a world-class redesign, treat the app shell and design tokens as the primary deliverable; redesign the system coherently rather than adding more page-level CSS overrides.
[2026-09-09] | I changed the application shell without invalidating the PWA cache, so the browser combined new navigation markup with an old stylesheet and rendered broken icons, overlaps, and obsolete colours. | Every PWA UI release must version static assets, advance the service-worker cache, remove obsolete caches, and use network-first delivery for mutable application assets before visual sign-off.
[2026-09-10] | I desaturated the supplied Pointlabs mark and allowed the mobile product lockup to appear undersized or wrap. | Preserve the original brand colours and validate every product lockup at desktop and mobile widths, including exact relative sizing and single-line behaviour.
[2026-09-10] | I ran credential-changing visual QA against the real local database and overwrote the administrator’s chosen password with the temporary test password. | Before any QA credential mutation, assert that the active database URI points to an explicitly created disposable database; never reset credentials in the user’s working database as a verification step.
[2026-09-10] | Authentication forms presented a generic User ID field while backend lookup accepted only the username, causing valid email-based login and OTP reset attempts to fail with a misleading raw error. | Resolve identity consistently by username or registered email across login and recovery, label accepted identifiers clearly, and render all expected auth failures inside the branded form.
